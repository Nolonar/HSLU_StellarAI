"""Supervisor state machine.

Supervisor oversees the interaction between the robot (physical) and the
computer (logical). It maintains a state machine about the possible states
that the robot can be in.

NOTE: Currently, the state machine is overly simplistic for demo purposes.
"""


class Supervisor(object):
    pass
